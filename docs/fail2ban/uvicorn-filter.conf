[Definition]
# Matches uvicorn log lines with HTTP 401 or 403 statuses
# Example: INFO:     127.0.0.1:40626 - "POST /works/upload HTTP/1.1" 403 Forbidden
failregex = ^.*\" (401|403) .*$
ignoreregex =
